{
  "name": "@pentaho/di-plugin-clusters-browse",
  "version": "${project.version}",
  "description": "${project.description}",
  "paths": {
    "pentaho/di/plugin/clusters-browse": "/"
  },
  "packages": [
    {
      "name": "pentaho/di/plugin/clusters-browse",
      "main": "app.module.js"
    }
  ],
  "dependencies": {
    "angular": "${angular.version}",
    "angular-ui-router": "${angular-ui-router.version}",
    "angular-i18n": "${angular-i18n.version}",
    "require-css": "${require-css.version}",
    "requirejs-text": "${requirejs-text.version}"
  },
  "config": {
    "pentaho/modules": {
      "pentaho/di/plugin/clusters-browse/services/providers/clusters.service": {"type": "IPenFileService"}
    }
  }
}